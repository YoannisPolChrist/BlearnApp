/**
 * Device admin notes for Blearn.
 *
 * This repo does not currently ship an active device-admin receiver.
 * If uninstall protection is revisited, keep it in the `app.blearn.mobile`
 * package and route all user-facing copy through Android string resources.
 *
 * Suggested files:
 * - `android/app/src/main/java/app/blearn/mobile/BlearnDeviceAdminReceiver.kt`
 * - `android/app/src/main/res/xml/device_admin_policies.xml`
 */

export {};
